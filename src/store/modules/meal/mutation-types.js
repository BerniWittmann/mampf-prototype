/* ============
 * Mutation types for the meal module
 * ============
 *
 * The mutation types that are available
 * on the meal module.
 */

export const ADD = 'ADD';
export const STORE = 'STORE';
export const SET_CURRENT = 'SET_CURRENT';

export default {
  ADD,
  STORE,
  SET_CURRENT,
};
